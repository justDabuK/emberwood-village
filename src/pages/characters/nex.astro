---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Nex from "../../components/character/Nex.vue";
import { getCollection } from "astro:content";

const allSpells = await getCollection("spells");
---

<!--
TODO:
- add nex
  - add spell overview for nex
    - concentrated spells have different background
    - there is a vue component for tracking
      - concentration
      - spell slots
    - when hovering over spell detail view, it extends and shows whole description
-->
<BaseLayout title="Nex" icon="dominik-profile-picture.jpg" iconCover>
  <Nex client:load allSpells={allSpells} />
</BaseLayout>
